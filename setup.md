# Setup 
```shell
git clone https://github.com/elarasu/vagrant-ubuntu-docker.git
cd vagrant-ubuntu-docker
# follow instructions in readme.md

vagrant ssh
git clone https://github.com/elarasu/emacs.git
# follow instructions in readme
```
## Git setup
```
git config --global user.email "elarasu@outlook.com"
git config --global user.name "el arasu"
```
